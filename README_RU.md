# 🎬 FutLive Player V2

Telegram бот для просмотра футбольных трансляций с встроенным плеером. Получайте прямые ссылки на Ace Stream и веб-плееры без посещения сайтов.

## ✨ Особенности

- 🤖 **Telegram Bot** - удобный интерфейс для выбора матчей
- 📱 **Встроенный плеер** - смотрите трансляции прямо в Telegram
- 🎬 **Поддержка форматов** - Ace Stream, HLS, DASH, MP4
- 🔄 **Автоматический retry** - система автоматически переходит на другой канал при ошибке
- 🌐 **Несколько прокси** - резервные прокси для Ace Stream
- 🚀 **Быстрая загрузка** - кэширование данных на 5 минут
- 🔐 **Безопасно** - прямые ссылки без посредников

## 🏗️ Архитектура

```
Telegram User
    ↓
Telegram Bot (bot_final.py)
    ↓
API Server (api_server.py)
    ↓
Parser (parser.py) → gooool365.org
    ↓
Web App (React) → Video.js Player
```

## 🚀 Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

```bash
cd /home/ubuntu/futlive-player-v2
./start_all.sh
```

Этот скрипт запустит все компоненты:
- API сервер на `http://localhost:5000`
- Web App на `http://localhost:3000`
- Telegram Bot

### Вариант 2: Ручной запуск

**Терминал 1 - API сервер:**
```bash
cd /home/ubuntu/futlive-player-v2
python3 api_server.py
```

**Терминал 2 - Web App:**
```bash
cd /home/ubuntu/futlive-player-v2
pnpm run dev
```

**Терминал 3 - Telegram Bot:**
```bash
cd /home/ubuntu/futlive-player-v2
python3 bot_final.py
```

## 📖 Использование

### В Telegram

1. Найдите бота: поиск по `@FutLiveBot` или используйте ссылку
2. Отправьте команду `/start`
3. Выберите матч из списка
4. Нажмите "📱 Смотреть в приложении"
5. Смотрите трансляцию в встроенном плеере

### Управление плеером

- **Переключение каналов**: нажимайте кнопки "Предыдущий" / "Следующий" или номер канала
- **Полноэкран**: нажмите кнопку полноэкрана в плеере
- **Громкость**: используйте ползунок громкости
- **Скорость воспроизведения**: выберите скорость (0.5x, 1x, 1.5x, 2x)

## 🔧 Конфигурация

### Telegram Bot Token

Отредактируйте `bot_final.py`:
```python
API_TOKEN = "8111388773:AAFiCTukv5d8XSavnsL7ybMs8kRL42uFWB4"
```

### API URL

Отредактируйте `bot_final.py`:
```python
API_BASE_URL = "https://futlive-player-v2.manus.space/api"
```

### Ace Stream прокси

Отредактируйте `client/src/lib/acestream.ts`:
```typescript
export const ACE_STREAM_PROXIES = [
  { name: 'proxy1', url: 'https://...', format: 'id', priority: 1 },
  // ...
];
```

## 🧪 Тестирование

### Проверка API

```bash
# Здоровье API
curl http://localhost:5000/api/health

# Список матчей
curl http://localhost:5000/api/matches

# Каналы для матча
curl http://localhost:5000/api/channels/0

# Конкретный канал
curl http://localhost:5000/api/channel/0/0
```

### Проверка Web App

Откройте в браузере:
```
http://localhost:3000/player?match_id=0
```

## 📋 Файловая структура

```
futlive-player-v2/
├── bot_final.py              # Telegram Bot
├── api_server.py             # REST API сервер
├── parser.py                 # Парсер gooool365.org
├── start_all.sh              # Скрипт запуска всех компонентов
├── stop_all.sh               # Скрипт остановки всех компонентов
├── INTEGRATION_GUIDE.md      # Подробное руководство интеграции
├── README_RU.md              # Этот файл
├── client/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Home.tsx      # Главная страница
│   │   │   └── Player.tsx    # Страница плеера
│   │   ├── components/
│   │   │   └── VideoJsPlayer.tsx  # Компонент Video.js плеера
│   │   ├── lib/
│   │   │   ├── api.ts        # API клиент
│   │   │   └── acestream.ts  # Утилиты Ace Stream
│   │   ├── App.tsx           # Главное приложение
│   │   └── index.css         # Глобальные стили
│   ├── public/               # Статические файлы
│   └── index.html            # HTML шаблон
├── package.json              # Node.js зависимости
└── logs/                      # Логи сервисов (создается при запуске)
```

## 🐛 Решение проблем

### Bot не отвечает

1. Проверьте токен в `bot_final.py`
2. Убедитесь, что бот запущен: `ps aux | grep bot_final.py`
3. Проверьте логи: `tail -f logs/telegram-bot.log`

### API сервер не доступен

1. Проверьте, что сервер запущен: `ps aux | grep api_server.py`
2. Проверьте порт 5000: `lsof -i :5000`
3. Проверьте логи: `tail -f logs/api-server.log`

### Плеер не загружается

1. Проверьте консоль браузера (F12)
2. Убедитесь, что API сервер доступен
3. Проверьте URL матча в API ответе
4. Попробуйте другой прокси вручную

### Парсер не находит матчи

1. Проверьте, что сайт gooool365.org доступен
2. Убедитесь, что структура сайта не изменилась
3. Обновите селекторы в `parser.py`
4. Проверьте логи парсера

## 📊 Мониторинг

### Просмотр логов в реальном времени

```bash
# Все логи
tail -f logs/*.log

# Только бот
tail -f logs/telegram-bot.log

# Только API
tail -f logs/api-server.log

# Только Web App
tail -f logs/web-app.log
```

### Проверка статуса процессов

```bash
# Все процессы FutLive
ps aux | grep -E "(bot_final|api_server|vite)"

# Проверка портов
lsof -i :5000    # API сервер
lsof -i :3000    # Web App
```

## 🔐 Безопасность

- Токен бота хранится в переменной окружения
- API сервер использует CORS для контроля доступа
- Все запросы логируются
- Нет хранения пользовательских данных

## 📦 Зависимости

### Python
- `aiogram` - Telegram Bot Framework
- `flask` - Web Framework
- `flask-cors` - CORS поддержка
- `requests` - HTTP клиент
- `beautifulsoup4` - HTML парсер

### Node.js
- `react` - UI фреймворк
- `vite` - Build tool
- `video.js` - Видео плеер
- `tailwindcss` - CSS фреймворк
- `typescript` - Type checking

## 🚀 Развертывание

### На сервере (systemd)

```bash
# Создать сервис для бота
sudo nano /etc/systemd/system/futlive-bot.service

[Unit]
Description=FutLive Telegram Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/futlive-player-v2
ExecStart=/usr/bin/python3 /home/ubuntu/futlive-player-v2/bot_final.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

# Включить и запустить
sudo systemctl enable futlive-bot
sudo systemctl start futlive-bot
```

### На Heroku

```bash
# Создать Procfile
echo "web: python3 bot_final.py" > Procfile

# Развернуть
heroku create futlive-player
git push heroku main
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `tail -f logs/*.log`
2. Убедитесь, что все компоненты запущены
3. Проверьте сетевое соединение
4. Прочитайте `INTEGRATION_GUIDE.md` для подробной информации

## 📝 Лицензия

MIT License

## 🙏 Благодарности

- [aiogram](https://github.com/aiogram/aiogram) - Telegram Bot Framework
- [Video.js](https://videojs.com/) - HTML5 Video Player
- [Flask](https://flask.palletsprojects.com/) - Web Framework
- [React](https://react.dev/) - UI Library

---

**Версия:** 2.0.0  
**Последнее обновление:** 2024  
**Статус:** ✅ Полностью функционален
