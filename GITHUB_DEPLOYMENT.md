# 🚀 GitHub Actions + VPS Deployment Guide

Это полное руководство по настройке автоматического развертывания FutLive Player V2 через GitHub Actions на ваш VPS.

## 📋 Требования

- GitHub аккаунт
- VPS сервер (Ubuntu 20.04+) с доступом по SSH
- Домен для вашего приложения
- Базовые знания Git и Linux

## 🔧 Шаг 1: Подготовка VPS

### 1.1 Подключитесь к VPS по SSH

```bash
ssh root@YOUR_VPS_IP
```

### 1.2 Запустите скрипт инициализации

```bash
cd /tmp
wget https://raw.githubusercontent.com/yourusername/futlive-player-v2/main/scripts/setup-vps.sh
chmod +x setup-vps.sh
sudo ./setup-vps.sh
```

Скрипт установит:
- Docker и Docker Compose
- Node.js и pnpm
- Redis
- Nginx
- SSL сертификаты (Let's Encrypt)

### 1.3 Отредактируйте конфигурацию

```bash
cd /home/ubuntu/futlive-player-v2
nano .env
```

Важные переменные:
```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here

# API Configuration
API_PORT=5000
API_HOST=0.0.0.0

# Redis
REDIS_URL=redis://redis:6379

# Sentry Monitoring
SENTRY_DSN=your_sentry_dsn_here

# Domain
DOMAIN_NAME=futlive.example.com

# Frontend
VITE_API_URL=https://futlive.example.com/api
```

## 🔑 Шаг 2: Настройка GitHub Secrets

### 2.1 Перейдите в настройки репозитория

1. Откройте ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions

### 2.2 Добавьте следующие Secrets

| Secret Name | Значение | Описание |
|------------|----------|---------|
| `VPS_HOST` | `123.45.67.89` | IP адрес вашего VPS |
| `VPS_USER` | `ubuntu` | SSH пользователь (обычно ubuntu) |
| `VPS_PORT` | `22` | SSH порт (обычно 22) |
| `VPS_SSH_KEY` | Содержимое приватного ключа | Приватный SSH ключ для подключения |
| `SLACK_WEBHOOK` | (опционально) | Webhook для уведомлений в Slack |

### 2.3 Как получить SSH ключ

**На вашем локальном компьютере:**

```bash
# Если у вас еще нет SSH ключа
ssh-keygen -t rsa -b 4096 -f ~/.ssh/github_deploy

# Скопируйте приватный ключ
cat ~/.ssh/github_deploy
```

**На VPS:**

```bash
# Добавьте публичный ключ
echo "your_public_key_content" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

## 📤 Шаг 3: Экспортируйте проект на GitHub

### 3.1 Создайте новый репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Назовите репозиторий: `futlive-player-v2`
3. Выберите "Private" (рекомендуется)
4. Нажмите "Create repository"

### 3.2 Загрузите проект

```bash
cd /home/ubuntu/futlive-player-v2

# Инициализируйте git (если еще не инициализирован)
git init

# Добавьте удаленный репозиторий
git remote add origin https://github.com/yourusername/futlive-player-v2.git

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: FutLive Player V2"

# Загрузите на GitHub
git branch -M main
git push -u origin main
```

## ✅ Шаг 4: Проверьте GitHub Actions

### 4.1 Откройте вкладку Actions

1. Перейдите в репозиторий на GitHub
2. Нажмите на вкладку "Actions"
3. Вы должны увидеть workflow "Deploy to VPS"

### 4.2 Проверьте статус

- 🟢 **Green** - Развертывание успешно
- 🟡 **Yellow** - Развертывание в процессе
- 🔴 **Red** - Ошибка развертывания

## 🔄 Как работает CI/CD Pipeline

### Автоматическое развертывание

Каждый раз, когда вы делаете `git push` в ветку `main`:

1. **Test** - GitHub Actions запускает тесты и проверяет TypeScript
2. **Deploy** - Если тесты прошли, автоматически развертывает на VPS

```bash
# Локально
git add .
git commit -m "Add new feature"
git push origin main

# GitHub Actions автоматически:
# 1. Клонирует репозиторий
# 2. Запускает тесты
# 3. Подключается к VPS по SSH
# 4. Обновляет код
# 5. Перезапускает Docker контейнеры
```

## 📊 Мониторинг развертывания

### Просмотр логов GitHub Actions

1. Перейдите в Actions → Deploy to VPS
2. Нажмите на последний workflow run
3. Нажмите на "deploy" job
4. Просмотрите логи развертывания

### Просмотр логов на VPS

```bash
# Логи всех контейнеров
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f api
docker-compose logs -f bot
docker-compose logs -f web
```

## 🛠️ Команды для управления на VPS

```bash
cd /home/ubuntu/futlive-player-v2

# Просмотр статуса контейнеров
docker-compose ps

# Перезапуск контейнеров
docker-compose restart

# Остановка контейнеров
docker-compose down

# Запуск контейнеров
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Обновление вручную (если нужно)
git pull origin main
docker-compose down
docker-compose up -d
```

## 🔒 Безопасность

### Лучшие практики

1. **Используйте SSH ключи вместо паролей**
   ```bash
   # Отключите вход по паролю
   sudo nano /etc/ssh/sshd_config
   # PasswordAuthentication no
   sudo systemctl restart ssh
   ```

2. **Ограничьте доступ по SSH**
   ```bash
   # Только из определенных IP
   sudo ufw allow from 203.0.113.0 to any port 22
   ```

3. **Используйте Firewall**
   ```bash
   sudo ufw enable
   sudo ufw allow 22/tcp
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   ```

4. **Обновляйте систему регулярно**
   ```bash
   sudo apt-get update
   sudo apt-get upgrade
   ```

## 🐛 Решение проблем

### GitHub Actions не может подключиться к VPS

**Проблема:** `Permission denied (publickey)`

**Решение:**
1. Проверьте, что SSH ключ добавлен на VPS
2. Проверьте, что `VPS_SSH_KEY` Secret содержит приватный ключ (не публичный)
3. Убедитесь, что права на файл: `chmod 600 ~/.ssh/authorized_keys`

### Docker контейнеры не запускаются

**Проблема:** `docker: command not found`

**Решение:**
```bash
# Переустановите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker ubuntu
```

### SSL сертификат не работает

**Проблема:** `ERR_SSL_PROTOCOL_ERROR`

**Решение:**
```bash
# Обновите сертификат
sudo certbot renew --force-renewal

# Перезагрузите Nginx
sudo systemctl restart nginx
```

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи GitHub Actions
2. Проверьте логи на VPS: `docker-compose logs -f`
3. Создайте Issue в репозитории GitHub

## 🎉 Готово!

Теперь ваш проект автоматически развертывается на VPS при каждом push в main ветку!

```
🚀 Ваше приложение доступно по адресу: https://futlive.example.com
```
