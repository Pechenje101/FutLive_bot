# Redis Кэширование и Система Уведомлений

Полное руководство по использованию Redis кэширования и системы уведомлений о матчах в FutLive Player V2.

## 📋 Содержание

1. [Redis Кэширование](#redis-кэширование)
2. [Система Уведомлений](#система-уведомлений)
3. [Интеграция с Telegram Ботом](#интеграция-с-telegram-ботом)
4. [Тестирование](#тестирование)
5. [Решение Проблем](#решение-проблем)

---

## 🔴 Redis Кэширование

### Что такое Redis?

**Redis** - это высокоскоростная база данных в памяти, которая используется для кэширования данных. Это позволяет:

- ⚡ **Быстрый доступ** - данные хранятся в памяти, а не на диске
- 📊 **Масштабируемость** - поддержка большого количества одновременных пользователей
- 💾 **Эффективность** - снижение нагрузки на парсер и API сервер
- 🔄 **TTL (Time To Live)** - автоматическое удаление устаревших данных

### Компоненты Redis

#### `redis_cache.py` - Класс RedisCache

Основной класс для работы с Redis кэшем:

```python
from redis_cache import get_cache

cache = get_cache()

# Сохранить матчи
cache.set_matches(matches, ttl=300)

# Получить матчи
matches = cache.get_matches()

# Добавить в избранное
cache.add_favorite(user_id=123, match_id=0)

# Получить избранные
favorites = cache.get_favorites(user_id=123)
```

### Функции Redis Кэша

#### Матчи

```python
# Сохранить матчи в кэш (TTL = 5 минут)
cache.set_matches(matches, ttl=300)

# Получить матчи из кэша
matches = cache.get_matches()

# Удалить матчи из кэша
cache.delete_matches()
```

#### Каналы

```python
# Сохранить каналы матча
cache.set_channels(match_id=0, channels={'Канал 1': 'url1'}, ttl=300)

# Получить каналы матча
channels = cache.get_channels(match_id=0)

# Удалить каналы матча
cache.delete_channels(match_id=0)
```

#### Избранные Матчи

```python
# Добавить матч в избранное
cache.add_favorite(user_id=123, match_id=0)

# Удалить матч из избранного
cache.remove_favorite(user_id=123, match_id=0)

# Получить все избранные матчи пользователя
favorites = cache.get_favorites(user_id=123)
```

#### Уведомления

```python
# Добавить уведомление
cache.add_notification(
    user_id=123,
    match_id=0,
    match_title='Матч 1',
    notify_time=1234567890  # Unix timestamp
)

# Получить ожидающие уведомления
pending = cache.get_pending_notifications()

# Отметить как отправленное
cache.mark_notification_sent(user_id=123, match_id=0)

# Удалить уведомление
cache.delete_notification(user_id=123, match_id=0)
```

### Проверка Статуса Redis

```python
# Проверить, подключен ли Redis
if cache.is_connected():
    print("✅ Redis подключен")
else:
    print("⚠️ Используется локальный кэш")

# Получить статистику
stats = cache.get_stats()
print(stats)
```

---

## 🔔 Система Уведомлений

### Что такое Система Уведомлений?

Система уведомлений автоматически отправляет напоминания пользователям за 15 минут до начала матча.

### Компоненты

#### `notifications_service.py` - Класс NotificationService

```python
from notifications_service import get_notification_service

service = get_notification_service()

# Установить callback для отправки уведомлений
async def send_notification(notification):
    print(f"Отправляю уведомление: {notification}")

service.set_notification_callback(send_notification)

# Добавить напоминание
service.add_match_reminder(
    user_id=123,
    match_id=0,
    match_title='Матч 1',
    match_start_time=1234567890  # Unix timestamp
)

# Запустить сервис
await service.start()
```

### Функции Сервиса Уведомлений

#### Добавление Напоминания

```python
# Добавить напоминание за 15 минут до матча
service.add_match_reminder(
    user_id=123,
    match_id=0,
    match_title='Матч 1',
    match_start_time=int(time.time()) + 3600  # Матч через 1 час
)
```

#### Удаление Напоминания

```python
# Удалить напоминание
service.remove_match_reminder(user_id=123, match_id=0)
```

#### Проверка и Отправка Уведомлений

```python
# Проверить и отправить ожидающие уведомления
sent_count = await service.check_and_send_notifications()
print(f"Отправлено {sent_count} уведомлений")
```

#### Запуск Сервиса

```python
# Запустить сервис (будет проверять каждые 60 секунд)
await service.start()

# Остановить сервис
service.stop()
```

---

## 🤖 Интеграция с Telegram Ботом

### Новые Функции Бота

#### 1. Избранные Матчи

```
⭐ Добавить в избранное
❌ Удалить из избранного
📋 Мои избранные
```

**Использование:**
- Нажмите "⭐ Добавить в избранное" при просмотре матча
- Нажмите "📋 Мои избранные" в главном меню для просмотра

#### 2. Напоминания о Матчах

```
🔔 Напоминание
```

**Использование:**
- Нажмите "🔔 Напоминание" при просмотре матча
- Вы получите уведомление за 15 минут до начала матча

#### 3. Redis Кэширование

Все данные автоматически кэшируются в Redis:
- Матчи кэшируются на 5 минут
- Каналы кэшируются на 5 минут
- Избранные матчи хранятся 30 дней
- Уведомления хранятся до отправки

### Код Интеграции

```python
# Добавить в избранное
@dp.callback_query(F.data.startswith("add_favorite_"))
async def add_favorite(query: types.CallbackQuery):
    match_id = int(query.data.split("_")[2])
    user_id = query.from_user.id
    cache.add_favorite(user_id, match_id)
    await query.answer("⭐ Матч добавлен в избранное!")

# Установить напоминание
@dp.callback_query(F.data.startswith("remind_"))
async def set_reminder(query: types.CallbackQuery):
    match_id = int(query.data.split("_")[1])
    user_id = query.from_user.id
    notification_service.add_match_reminder(
        user_id=user_id,
        match_id=match_id,
        match_title=match['title'],
        match_start_time=int(time.time()) + 3600
    )
    await query.answer("✅ Напоминание установлено!")

# Запуск сервиса уведомлений
async def notification_loop():
    notification_service.set_notification_callback(send_notification)
    await notification_service.start()

# В main()
notification_task = asyncio.create_task(notification_loop())
```

---

## 🧪 Тестирование

### Тестирование Redis Кэша

```bash
# Запустить тесты кэша
cd /home/ubuntu/futlive-player-v2
python3 redis_cache.py
```

**Ожидаемый результат:**
```
=== Тестирование Redis кэша ===

1️⃣ Тест матчей:
✅ Матчи: 2 шт

2️⃣ Тест избранных:
✅ Избранные: [0, 1]

3️⃣ Тест уведомлений:
✅ Ожидающих уведомлений: 1 шт

4️⃣ Статистика:
✅ {'used_memory': '1.23M', 'connected': True, 'type': 'Redis'}
```

### Тестирование Сервиса Уведомлений

```bash
# Запустить тесты уведомлений
cd /home/ubuntu/futlive-player-v2
python3 notifications_service.py
```

**Ожидаемый результат:**
```
=== Тестирование сервиса уведомлений ===

1️⃣ Добавляем напоминание на 2024-01-14 12:30:00
2️⃣ Запускаем сервис (будет проверять каждые 1 сек)...

📬 УВЕДОМЛЕНИЕ:
   Пользователь: 123
   Матч: Тестовый матч
   Время отправки: 2024-01-14 12:15:00

✅ Тестирование завершено
```

### Проверка Redis в Командной Строке

```bash
# Проверить, запущен ли Redis
redis-cli ping
# Ожидаемый результат: PONG

# Получить информацию о Redis
redis-cli info

# Просмотреть все ключи в кэше
redis-cli keys '*'

# Получить значение ключа
redis-cli get matches

# Очистить весь кэш
redis-cli flushdb

# Остановить Redis
redis-cli shutdown
```

---

## 🔧 Решение Проблем

### Redis не запускается

**Проблема:** `redis-cli: command not found`

**Решение:**
```bash
# Установить Redis
sudo apt-get install redis-server

# Проверить установку
redis-server --version
```

### Redis не подключается

**Проблема:** `Connection refused`

**Решение:**
```bash
# Запустить Redis вручную
redis-server

# Или запустить как демон
redis-server --daemonize yes

# Проверить статус
redis-cli ping
```

### Локальный кэш вместо Redis

**Проблема:** Система использует локальный кэш вместо Redis

**Решение:**
```bash
# Проверить, запущен ли Redis
redis-cli ping

# Если не запущен, запустить
redis-server --daemonize yes

# Перезапустить бота
./stop_all.sh
./start_all.sh
```

### Уведомления не отправляются

**Проблема:** Уведомления не приходят в Telegram

**Решение:**
1. Проверьте, запущен ли бот: `ps aux | grep bot_final.py`
2. Проверьте логи: `tail -f logs/telegram-bot.log`
3. Убедитесь, что токен бота правильный в `bot_final.py`
4. Проверьте, установлено ли напоминание: `redis-cli keys 'notifications:*'`

### Высокое использование памяти

**Проблема:** Redis использует много памяти

**Решение:**
```bash
# Проверить использование памяти
redis-cli info memory

# Очистить кэш
redis-cli flushdb

# Установить максимальный размер памяти в redis.conf
maxmemory 256mb
maxmemory-policy allkeys-lru
```

---

## 📊 Мониторинг

### Просмотр Статистики Redis

```bash
# Получить полную информацию
redis-cli info

# Получить информацию о памяти
redis-cli info memory

# Получить информацию о клиентах
redis-cli info clients

# Получить информацию о статистике
redis-cli info stats
```

### Просмотр Логов

```bash
# Логи Redis
tail -f logs/redis.log

# Логи API сервера
tail -f logs/api-server.log

# Логи Telegram бота
tail -f logs/telegram-bot.log

# Все логи
tail -f logs/*.log
```

### Проверка Производительности

```bash
# Тест скорости Redis
redis-benchmark -t get,set -n 100000

# Тест с пользовательскими параметрами
redis-benchmark -h localhost -p 6379 -c 50 -n 100000
```

---

## 🚀 Развертывание

### На Продакшене

1. **Установить Redis на сервере:**
```bash
sudo apt-get install redis-server
```

2. **Настроить Redis для продакшена:**
```bash
# Отредактировать /etc/redis/redis.conf
sudo nano /etc/redis/redis.conf

# Найти и изменить:
# bind 127.0.0.1 -> bind 0.0.0.0 (если нужен доступ с других машин)
# requirepass "" -> requirepass "your_password" (установить пароль)
# maxmemory 256mb (установить лимит памяти)
```

3. **Перезапустить Redis:**
```bash
sudo systemctl restart redis-server
```

4. **Обновить конфиг в `redis_cache.py`:**
```python
cache = RedisCache(
    host='localhost',
    port=6379,
    db=0,
    password='your_password'  # Если установлен пароль
)
```

---

## 📝 Примеры Использования

### Пример 1: Кэширование Матчей

```python
from redis_cache import get_cache
from parser_async import get_matches

cache = get_cache()

# Получить матчи с кэшированием
cached = cache.get_matches()
if not cached:
    matches = await get_matches()
    cache.set_matches(matches, ttl=300)
else:
    matches = cached

print(f"Матчей: {len(matches)}")
```

### Пример 2: Управление Избранными

```python
from redis_cache import get_cache

cache = get_cache()
user_id = 123

# Добавить в избранное
cache.add_favorite(user_id, match_id=0)
cache.add_favorite(user_id, match_id=1)

# Получить избранные
favorites = cache.get_favorites(user_id)
print(f"Избранные матчи: {favorites}")

# Удалить из избранного
cache.remove_favorite(user_id, match_id=0)
```

### Пример 3: Система Уведомлений

```python
from notifications_service import get_notification_service
import asyncio
import time

service = get_notification_service()

async def send_notification(notification):
    print(f"📬 Уведомление: {notification['match_title']}")

service.set_notification_callback(send_notification)

# Добавить напоминание на 1 час в будущем
match_start_time = int(time.time()) + 3600
service.add_match_reminder(
    user_id=123,
    match_id=0,
    match_title='Матч 1',
    match_start_time=match_start_time
)

# Запустить сервис
await service.start()
```

---

## 📚 Дополнительные Ресурсы

- [Redis Documentation](https://redis.io/documentation)
- [Redis Python Client](https://github.com/redis/redis-py)
- [Redis Commands](https://redis.io/commands)
- [Redis Data Types](https://redis.io/topics/data-types)

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024  
**Статус:** ✅ Полностью функционален
